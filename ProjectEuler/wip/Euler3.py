#author: Mike Zott
import numpy as np
#inp = 60
inp = 600851475143
primes = np.arange(int(np.sqrt(inp)))
    for i in range(len(primes)):
        if primes[i] != 0:
            for j in range(5)
            
        ninp /= i
        primes.append(i)
    print 'Finished percent %3.3f' % (float(i)/  inp * 100)
print primes
